Name:<%= @product.name %><br>
Category:<%= @product.category.name %><br>
Size:<%= @product.size.name %><br>
Color:<%= @product.color.name %><br>
Price:<%= @product.price %><br>
Description:<%= @product.description %><br>

<%= image_tag @product.image.url, width: '600px' if @product.image? %>

<%= link_to "List of Products", admin_designer_path(params[:designer_id]) %>