<% if item.product.thirtydays_status == true %>
<%= item.quantity %>
<% else %>
<%= form_for item do |f| %>
<%= f.number_field :quantity, max: item.inventory.amount, min: 1, class: "form-control" %>
<%= f.submit 'Update', class: "mt-3 btn btn-primary" %>
<% end %>
<% end %>